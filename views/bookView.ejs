<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= book.title %> - Book Details</title>
  <link rel="stylesheet" href="/css/tailwind.css" />
  <script src="https://unpkg.com/lucide@latest"></script>
</head>

<body class="bg-[oklch(0.97_0.01_70)] text-slate-900 min-h-screen flex flex-col items-center">
  
  <%- include('partials/navbar') %>

  <main class="container mx-auto p-6 w-full max-w-3xl mt-10">
    <div class="bg-white p-8 rounded-2xl shadow border border-gray-200">
        <a href="/books" 
        class="  text-teal-700 hover:text-teal-800 transition">
       <i data-lucide="arrow-left" class="w-6 h-6"></i>
     </a>
      <div class="flex flex-col items-center text-center">
        <i data-lucide="book-open" class="w-16 h-16 mb-6 text-teal-700"></i>
        <h1 class="text-3xl font-bold text-teal-700 mb-2"><%= book.title %></h1>
        <p class="text-gray-600 mb-4">
          by 
          <a href="../authors/<%= book.author_id %>" class="font-semibold text-slate-800">
           <%= book.author_name %>
           
        </a>
        </p>
      </div>

      <div class="text-gray-700 text-base leading-relaxed text-center mb-8">
        <p><%= book.description || "No description available for this book." %></p>
      </div>
      <% if (book.pdf_path) { %>
        <div class="flex justify-center mb-6">
          <a 
            href="/books/<%= book.id %>/download" 
            class="inline-flex items-center gap-2 px-6 py-3 bg-teal-700 text-white rounded-lg hover:bg-teal-800 transition-colors shadow-md hover:shadow-lg"
          >
            <i data-lucide="download" class="w-5 h-5"></i>
            <span>Download PDF</span>
          </a>
        </div>
        <% } %>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 text-center">
        <div class="bg-[oklch(0.97_0.01_70)] rounded-xl p-4 shadow-sm border border-gray-200">
          <div class="text-gray-500 text-sm mb-1">Publisher</div>
          <div class="font-medium text-slate-800 opacity-60"><%= book.publisher_name %></div>
        </div>
        <div class="bg-[oklch(0.97_0.01_70)] rounded-xl p-4 shadow-sm border border-gray-200">
          <div class="text-gray-500 text-sm mb-1 ">Published date</div>
          <div class="font-medium text-slate-800 opacity-60"><%= new Date(book.created_at).toLocaleDateString('en-GB') || "Unknown" %></div>
        </div>
      </div>
    </div>
  </main>
  <script>
    document.addEventListener("DOMContentLoaded", () => lucide.createIcons());
  </script>
</body>
</html>
